/** Canonical external destinations for Community / ops surfaces. */

/** Hosted uptime + incidents (Better Stack). Independent of this site’s host. */
export const STATUS_MONITOR_URL = "https://status.devcentr.org";

/**
 * Public Slack invite (never-expiring preferred).
 * Slack → workspace menu → Invite people → Copy invite link.
 * Replace this once you have the real join URL.
 */
export const SLACK_INVITE_URL = "https://devcentr.slack.com";
