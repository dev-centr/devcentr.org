link:https://devcentr.org[image:https://img.shields.io/badge/site-devcentr.org-2ea043?style=flat[devcentr.org]]

= devcentr.org

The official organization landing page for link:https://github.com/dev-centr[Dev-Centr].

== Domain

* **URL:** link:https://devcentr.org[devcentr.org]
* **Hosting:** GitHub Pages (static build from this repo). CI deploys `.output/public` on every push to `main` via `.github/workflows/ci.yml`. Custom domain is configured on the repo's Pages settings.

== Brand

Site copies live in `public/brand/`. Canonical org-wide assets (including `logo.svg`) are harbored in the `.github` / `.github-private` profile folders. Upload `logo-on-dark-256.png` as the GitHub organization avatar under org Settings → Profile.

== Blog

* **URL:** link:https://devcentr.org/blog[/blog]
* **Source:** AsciiDoc posts in `content/blog/`
* **Build:** `pnpm run blog:build` (also runs on `predev` / `prebuild`) writes `src/lib/blog-posts.generated.json`

== Stack

* **Framework:** link:https://start.solidjs.com/[SolidStart] (Solid.js full-stack)
* **Engine:** link:https://vinxi.vercel.app/[Vinxi]
* **Language:** TypeScript
* **Package Manager:** pnpm

== Getting Started

[source,bash]
----
pnpm install
pnpm dev
----

== Project Structure

* `src/` — Source code (scaffolded)
* `content/blog/` — AsciiDoc chronicle posts
* `public/` — Static assets
* `app.config.ts` — SolidStart/Vinxi configuration
* `DESIGN.md` — Design philosophy and guidelines

== Site Generation

To iterate on the design, provide the `SITE-GENERATION-PROMPT.md` to a design-capable AI.
